- @group = GroupDecorator.decorate(@group)
= content_for :no_container do
  #heading.user
    .container
      .display= render 'groups/teaser'
      .edit{ style: 'display:none;' }

  #content
    .container
      = yield

= render template: 'layouts/application'