- @promotion = GroupDecorator.decorate(@promotion)
= content_for :no_container do
  #heading.user
    .container
      .display= render "assignments/teaser"
      .edit{ style: 'display:none;' }

  #content
    .container
      = yield

= render template: 'layouts/application'