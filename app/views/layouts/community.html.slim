- @community = GroupDecorator.decorate(@community)
= content_for :no_container do
  #heading.user.with-top-container
    .display= render "groups/communities/teaser"
    .container
      .edit  style='display:none;'

  #content
    .container
      = yield

= render template: "layouts/#{current_layout}"
