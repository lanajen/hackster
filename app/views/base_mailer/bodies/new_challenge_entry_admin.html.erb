<p>Hello there,</p>

<p>This is to let you know that <a href="|author_link|">|author_name|</a> posted a <a href="|project_link|">new entry</a> for <a href="<%= u.challenge_url(@context[:challenge], utm_source: 'hackster', utm_medium: 'email', utm_campaign: @context[:email_template]) %>">|challenge_name|</a>.</p>

<p>You can moderate it by going to the <a href="<%= u.challenge_admin_entries_url(@context[:challenge]) %>">entries dashboard</a>.</p>

<%= render 'signature' %>