<p>Hello there,</p>

<p>This is to let you know that <a href="<%= u.challenge_url(@context[:challenge], utm_source: 'hackster', utm_medium: 'email', utm_campaign: @context[:email_template]) %>">|challenge_name|</a> has launched and is now open for submissions. It will run until <%= l @context[:challenge].end_date.in_time_zone(PDT_TIME_ZONE) %> PDT.</p>

<%= render 'signature' %>