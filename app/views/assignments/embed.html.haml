- if @projects.any?
  %h4= link_to @assignment.name, assignment_url(@assignment)
  - @projects.each do |project|
    = render partial: "projects/project_embed_thumb#{@list_style}", locals: { project: project.decorate, col: '3', ref: 'promotion' }
- else
  All student projects will appear here.