= react_component('MouserContest')

#mousercontest-landing
  section#hero
    | This is where the navigation and content information will go

  section#bracket
    | This is where the contest bracket will go

  section#vendors
    h2 Vendors
    ul
      - @platforms.each do |platform|
        li= link_to platform.name, mouser_vendor_path(platform.user_name)