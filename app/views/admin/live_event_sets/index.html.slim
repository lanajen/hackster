- model_class = LiveEventSet
.page-header
  h1
    =t '.title', :default => model_class.model_name.human.pluralize
    small
      ==> link_to t('.new', :default => t("helpers.links.new")), new_admin_live_event_set_path, :class => 'btn btn-primary btn-sm'

==> render 'admin/shared/search_form_tag'
- (@fields.keys - %w(created_at event_type)).each do |field|
  ==> render partial: 'admin/shared/search_form_text_field', locals: { field: field }
==> render partial: 'admin/shared/search_form_select_field', locals: { field: 'event_type', options: LiveEventSet::EVENT_TYPES }
= render 'admin/shared/search_form_sort_and_submit'

table.table.table-striped
  thead
    tr
      th= model_class.human_attribute_name(:name)
      th= model_class.human_attribute_name(:event_type)
      th= model_class.human_attribute_name(:location)
      th=t '.actions', :default => t("helpers.actions")
  tbody
    - @live_event_sets.each do |live_event_set|
      tr
        td= live_event_set.name
        td= live_event_set.event_type
        td= live_event_set.decorate.location
        td
          ==> link_to t('.edit', :default => t("helpers.links.edit")), edit_admin_live_event_set_path(live_event_set), :class => 'btn btn-default btn-xs'
          = link_to t('.destroy', :default => t("helpers.links.destroy")), admin_live_event_set_path(live_event_set.id), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, :class => 'btn btn-xs btn-danger'

.pagination.col-xs-12.text-center
  = will_paginate @live_event_sets
