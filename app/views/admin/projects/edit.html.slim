.page-header
  h1 Editing #{@base_article.name}

= simple_nested_form_for [:admin, @base_article], url: admin_project_path(@base_article.id), html: { class: 'form-horizontal accept-file-upload' }, wrapper: :bootstrap3_horizontal do |f|
  = render partial: 'form', locals: { f: f }