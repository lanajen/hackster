<%- if @refresh %>
  window.location.replace('<%= url_for(@project) %>');
<% else %>
  $('.project .display').html('<%= escape_javascript render('teaser_normal') %>');
  $('.project .edit').slideUp(function(){
    $('.project .display').slideDown();
  });
  $('.istooltip').tooltip();
<% end %>