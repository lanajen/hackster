$(function(){
  $('.file-drop<%= ".#{file_type}" if defined?(file_type) %>').on('click', 'a', function(e){
    e.preventDefault();
    $($(this).data('target')).find('input[type=file]').click();
  });
});