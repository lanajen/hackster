.project-thumb-container#submit-idea-thumb
  - cache "tech-#{tech.id}-submit-idea", tag: ["tech-#{tech.id}-submit-idea", 'tech-submit-idea'], expires_in: 24.hours do
    div  class="submit-idea"
      .thumb-vertical  class="col-md-#{ defined?(col) ? col : 3 } col-xs-12 col-sm-6"
        .project-thumb
          .thumb
            .inner-thumb
              h3= tech.project_ideas_phrasing
              p= link_to "#{content_tag(:i, '', class: 'fa fa-plus')} Post your project idea".html_safe, '', class: 'btn btn-primary btn-sm'
            .photo-thumb
              = link_to '', '', style: "background-image:url(#{tech.cover_image.try(:file_url) || tech.avatar.try(:file_url, :big)});"

      .col-xs-12.thumb-horizontal
        .project-thumb-horizontal.project-thumb
          .thumb.media
            = link_to image_tag(tech.cover_image.try(:file_url) || tech.avatar.try(:file_url, :big), alt: tech.name, class: 'media-object'), '', class: 'pull-right photo-thumb'
            .inner-thumb.media-body
              h3 No #{tech.name} yet?
              p= link_to "#{content_tag(:i, '', class: 'fa fa-plus')} Post your project idea".html_safe, '', class: 'btn btn-primary btn-sm'

- link = user_signed_in? ? new_project_path(project: { tech_tags_string: @tech.tech_tags.first.name, mark_as_idea: true }) : new_user_registration_path(reason: 'project', m: 'group', id: @tech.id, redirect_to: new_project_path(project: { tech_tags_string: @tech.tech_tags.first.name, mark_as_idea: true }))
javascript:
  $('#submit-idea-thumb a').attr('href', '#{link.html_safe}');