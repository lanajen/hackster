= link_to "#{content_tag(:i, '', class: 'icon-comment')} #{size}".html_safe, '#', class: 'btn btn-mini expand', title: 'Comment', data: { target: "#widget-#{widget.id}-comments" }, id: "widget-#{widget.id}-comments-button"