.col-xs-5.col-xs-offset-3
  .wellified
    %h2.text-center You're on the list!
    %p
      %strong Want to get in faster? Show us what you've built
    = simple_form_for @invite_request do |f|
      = f.simple_fields_for :project do |f_project|
        = f_project.input :name, label: 'What is it called?', required: false
        = f_project.input :one_liner, as: :text, input_html: { rows: 4 }, label: 'One-liner description, Twitter-style.', required: false
        - if @invite_request.project.logo and @invite_request.project.logo.file_url(:thumb)
          .form-group
            .controls
              = image_tag @invite_request.project.cover_image.file_url(:cover_thumb)
        = f_project.simple_fields_for :cover_image do |f_logo|
          = f_logo.input :file, as: :file, label: 'Cover image (i.e your nicest picture)', required: false
          = f_logo.input :file_cache, as: :hidden
      = f.button :submit, 'Get in faster', class: 'btn-block btn btn-default'