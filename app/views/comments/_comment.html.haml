.comment
  .pull-left.comment-avatar
    = comment.user.decorate.avatar_link(:mini)
  .pull-left.comment-text
    = link_to comment.user.decorate.name_link
    = comment.body.html_safe
    .muted.comment-date
      = time_ago_in_words(comment.created_at)
      ago
  - if can? :destroy, comment
    = link_to 'Delete', comment_path(comment), method: :delete, data: { remote: true, confirm: 'Are you sure you want to delete this comment?', hover: '.comment' }, class: 'btn-heading btn-delete hidden'