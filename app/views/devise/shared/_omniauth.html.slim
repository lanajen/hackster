- (Devise.omniauth_providers - [:arduino, :saml]).sort.each do |provider|
  = link_to proper_name_for_provider(provider), user_omniauth_authorize_url(provider, host: APP_CONFIG['default_host'], protocol: request.scheme, current_site: current_site.try(:subdomain), login_locale: I18n.locale, setup: true, redirect_to: @redirect_to || params[:redirect_to] || (is_trackable_page? ? request.path : nil), user: ENV['SITE_USERNAME'], password: ENV['SITE_PASSWORD']), class: "zocial #{css} #{zocial_class_for_provider(provider)}"