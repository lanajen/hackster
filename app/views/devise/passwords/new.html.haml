.span3.offset4.wellified
  %h2{ style: 'text-align:center;' } Forgot your password?
  = simple_form_for(resource, :as => resource_name, :url => password_path(resource_name), :html => { :method => :post }) do |f|
    = f.error_notification
    = f.input :email, required: true, label: false, placeholder: 'Your email'
    = f.button :submit, "Reset my password", class: 'btn btn-block'

  %p
    Already have an account?
    = link_to 'Sign in.', new_user_session_path
  %p
    Need an invite?
    = link_to 'Request one.', root_url(subdomain: 'www'), onclick: "_gaq.push(['_trackEvent', 'Sign up form', 'Request an invite', '#{request.path}']);"